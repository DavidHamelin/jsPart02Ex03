// Déclaration de la fonction affichant une boite de dialogue quand on appuie sur une touche 
function pushMe()
{
    // On récupère ce qui est saisie dans l'input
    var name = document.getElementById("lastname").value;
    alert(name);
}